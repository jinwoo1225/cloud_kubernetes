apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-monitor.fullname" . }}
  labels:
    {{- include "log-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-monitor.selectorLabels" . | nindent 4 }}
