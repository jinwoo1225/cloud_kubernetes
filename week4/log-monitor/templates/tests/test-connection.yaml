apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-monitor.fullname" . }}-test-connection"
  labels:
    {{- include "log-monitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-monitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
