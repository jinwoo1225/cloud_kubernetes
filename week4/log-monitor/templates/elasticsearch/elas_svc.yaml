apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.elasticsearch.name}}-service
  labels:
    app: {{ .Values.elasticsearch.name}}-service
spec:
  clusterIP: None
  selector:
    app: {{ .Values.elasticsearch.name}}-service
  ports:
  - port: 9200
    name: rest
  - port: 9300
    name: inter-node  
